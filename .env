# Production site URL. Drives the absolute Open Graph, Twitter, and canonical
# URLs that link previews require. The production domain is not chosen yet, so
# this is a sensible placeholder. Change this one line at launch.
VITE_SITE_URL=https://eight-letters.example.com
