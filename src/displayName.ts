/**
 * The app's display name, in one place. Anything that shows the name reads it
 * from here rather than hardcoding a copy, so it changes once and flows
 * everywhere that pulls from this constant, the daily share included.
 */
export const APP_DISPLAY_NAME = 'Peach of a Word';
