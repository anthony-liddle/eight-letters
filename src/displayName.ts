/**
 * The app's display name, in one place. A rename is in progress (Peach of a
 * Word, pending sign-off), so anything that shows the name reads it from here
 * rather than hardcoding a copy. When the new name lands, it changes once and
 * flows everywhere that pulls from this constant, the daily share included.
 */
export const APP_DISPLAY_NAME = '8 Letters in Search of a Word';
